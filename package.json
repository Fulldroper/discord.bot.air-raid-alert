{
  "name": "bot_engine",
  "version": "1.0.0",
  "description": "Simple engine for modern discord bot",
  "main": "index.js",
  "scripts": {
    "start": "npm i --omit=dev && node .",
    "worker": "clear && node .",
    "test": "nodemon --exec npm run worker",
    "deploy": "git add . && git commit -m 'upd' && git push deploy master && git push github master",
    "deploy-force": "git add . && git commit -m 'upd' && git push -f deploy master && git push -f github master"
  },
  "config": {
    "path": {
      "events": "./events/",
      "commands": "./commands/",
      "addons": "./addons/"
    }
  },
  "author": "full_droper <full_droper@pm.me>",
  "license": "MIT",
  "devDependencies": {
    "dotenv": "^16.0.1",
    "nodemon": "^2.0.16"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "discord.js": "^14.2.0",
    "fs": "^0.0.1-security",
    "mongodb": "^4.9.0"
  }
}
